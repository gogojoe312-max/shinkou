# 進行のセキュリティ構成

## 配信方式

GitHub Pages 版は既存端末の利用を継続するための互換版です。サーバー認証はありません。JSON の書き出しから設定内の秘密キーを除外し、取り込みで端末の同期先・資格情報を置換しません。同期先は非公開 GitHub リポジトリに限定します。公開共有の新規作成は無効です。

Render 版は同じ画面を使い、Google ID トークンの検証と明示的なメールアドレス許可リストでアクセスを制限します。未設定時はデータ API を閉じたままにします。管理者・編集者・閲覧者の権限は全 API で確認します。GitHub / OpenAI の秘密キーをブラウザーへ渡しません。

自動ロック、非操作時間によるログアウト、アプリ切り替えによるロックはありません。ログインの有効期間は発行から 30 日です。期限切れで編集中の入力は消しません。サーバー版では最初の接続・再読み込み時にオンラインでの認証確認が必要です。

## Render 設定

Node.js 22〜24。ビルド `npm ci --omit=dev --ignore-scripts`、起動 `npm start`。`PORT` を使用し、`0.0.0.0` で待ち受けます。ヘルスチェックは `/healthz`。Free では休止後の初回アクセスに待ち時間があります。

環境変数は Render の Environment から設定します。秘密キーをソース、チャット、スクリーンショットに貼らないでください。

| 変数 | 内容 |
| --- | --- |
| `SESSION_SECRET` | 暗号学的乱数 32 バイト以上。キャッシュ暗号化にも使用するため通常は固定 |
| `GOOGLE_CLIENT_ID` | Google Web OAuth クライアント ID。承認済み JavaScript 生成元に Render の HTTPS URL を登録 |
| `OWNER_EMAILS` | 管理者の正確なメールアドレス。カンマ区切り |
| `EDITOR_EMAILS` | 編集者。必要になるまで空欄 |
| `VIEWER_EMAILS` | 閲覧者。必要になるまで空欄 |
| `GITHUB_TOKEN` | データリポジトリのみを選んだ fine-grained PAT。Contents read/write と Metadata read のみ |
| `DATA_OWNER`, `DATA_REPO` | 固定の非公開データリポジトリ。クライアントから変更不可 |
| `DATA_PATH`, `DATA_BRANCH` | 既定値 `data.json`, `main` |
| `OPENAI_API_KEY` | このアプリ用のプロジェクトキー。利用上限・請求通知は OpenAI 側で設定 |
| `DROPBOX_APP_KEY` | 任意。Dropbox アプリの公開 App key |
| `PUBLIC_ORIGIN` | 任意。未指定時は `RENDER_EXTERNAL_URL`。独自ドメインでは正確な HTTPS 生成元を指定 |
| `SESSION_EPOCH` | 既定値 `1`。値を変更して再配信すると全ログインを無効化 |

Google ログインは確認済み Gmail アドレスまたは Workspace アカウントに限定しています。Gmail / Calendar 機能を使う場合は該当 API と同意画面も設定します。

Dropbox は `files.metadata.read` だけを許可し、Redirect URI に `https://<Renderのホスト>/oauth/dropbox/callback` を登録します。アプリ作成時の開発者規約とアクセス許可は利用者が確認します。共有フォルダを列挙するため Dropbox 側のアプリ種別は Full Dropbox が必要ですが、API 権限はメタデータの読み取りだけです。サーバーではさらに、保存済みの曲に登録された共有フォルダ URL だけを受け付けます。短期トークンはメモリにのみ保持し、サーバー再起動時には再接続が必要です。

## 保存と復元

端末の暗号化は AES-GCM。互換版でパスコードを設定・変更すると、現在データ、直前データ、同期の基準、世代・日次バックアップを一括で暗号化します。PBKDF2 SHA-256 は 600,000 回。古いバックアップを開けない場合や他のタブが更新した場合は全変更を取り消します。古いタブによる平文の上書きも拒否します。

Render 版は利用者ごとに保存領域と暗号鍵を分離します。鍵はログイン確認時だけ受け取り、端末には保存しません。端末を操作できる人、ブラウザー拡張機能、同一生成元で実行される不正スクリプトから、開いた画面を隠す仕組みではありません。

サーバーの更新履歴は更新者・日時を付けて非公開データリポジトリ内に保持します。更新した日は日本時間の日付で `snapshots/YYYY-MM-DD.json` を作成し、既存の同日コピーを上書きしません。管理者は設定の「保存とバックアップ」から戻せます。GitHub のリビジョン競合は拒否し、既存の 3 方向マージで再試行します。同じリポジトリのバックアップはアカウント喪失・リポジトリ全体の削除には対応しません。

ログアウトの失効記録とレート制限は単一サーバーのメモリに保持します。失効記録は再起動で消えるため、盗難セッション対応では `SESSION_EPOCH` を変更します。複数インスタンス構成にする場合は共有ストアへ移してください。

## 移行確認

1. 互換版の同期が完了してから Render 側を設定します。
2. 管理者としてログインし、曲・案件・請求書が表示されることを確認します。
3. 端末だけにある AI 相談履歴や設定は自動転送されません。必要な場合は互換版の JSON 書き出しを使って移行します（秘密キーは含みません）。
4. 全端末の移行後、不要になった端末用トークン・公開共有を見直します。既存のファイルや共有リンクをこの変更で勝手に削除・失効させません。

`npm ci --ignore-scripts && npm test` で保存・暗号化・認証・権限・CSRF・同期・Dropbox・AI・請求書の回帰テストを実行します。`npm audit --omit=dev` で本番依存関係を確認します。
